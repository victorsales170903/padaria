// schema.prisma
generator client {
  provider = "prisma-client-js"
  output   = "./generated/client"
}

datasource db {
  provider = "mysql"
  url      = "mysql://root:root@localhost:3306/padaria"
}

model pedidos {
  id    Int    @id @default(autoincrement())
  nome String 
  qtd_paes Int
}
